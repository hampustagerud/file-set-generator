# File set template generator

## Installation

```
$ yarn install
```

## Setup

Put template .docx files in `<root>/templates`.

## Generate a set

```
yarn create-set
```

Sets are generated into a `./generated` folder with a symlink to the
latest set 🎉
